---
// import type { PagePayload } from "../../types/index";
import type { PortableTextBlock } from "sanity";

type Props = {
  page: any;
};

const { page } = Astro.props;
---

<h1>{page.title}</h1>
<p>{page.slug}</p>
<p>{page.blueprint}</p>
<p>form</p>
